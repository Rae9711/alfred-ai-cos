# Alfred Preview UI

Runnable design-system reference (source of truth for Alfred mobile UI).

## Run

```bash
cd mobile/preview/alfred-preview-ui
npm install
npm run dev
```

Open the Vite URL (usually http://localhost:5173/).

Shows: three-tier icon styles, typography (Noto Serif SC / Georgia / DM Sans), color palette, and the Home phone mock.
